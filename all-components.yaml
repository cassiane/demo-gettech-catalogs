# all-components.yaml
# Adicione aqui as definições de componentes do Backstage
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: example-components
  description: A collection of all Backstage example components
spec:
  targets:
    - ./components/*.yaml